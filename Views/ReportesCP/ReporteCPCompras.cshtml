@model List<PROYECTO_WEB.Models.compras>
@{
    ViewBag.Title = "ReporteCPCompras";
}

<h2 style="color:white;">Compras</h2>

@using (Html.BeginForm("ReporteCPCompras_Click", "ReportesCP", FormMethod.Post))
{
    <div style="color:white;" class="form-group">
        <label style="color: white;">Datos de la compra</label>
    </div>
    <div class="form-group d-flex justify-content-between">
        <div class="col-sm-6 d-flex">
            @Html.TextBox("id_com", null, new { @class = "form-control", @placeholder = "Nombre", @autocomplete = "off" })
            <button style="border-color: transparent; --bs-btn-hover-bg: #1F618D; --bs-btn-color: #1F618D;" onclick="" type="submit" name="btn_enviar" value="Buscar Cliente" class="btn btn-outline-light"><i class="fa-solid fa-magnifying-glass"></i></button>
        </div>
    </div>
}

<<table class="table table-dark table-sm align-middle">
    <tr class="text-center">
        <th>
            NUMERO SERIE
        </th>
        <th>
            FECHA COMPRA
        </th>
        <th>
            MONTO
        </th>
        <th>
            EMPLEADO
        </th>
        <th>
            PROVEEDOR
        </th>
    </tr>

    @{
        if (Model.Count() != 0 || Model != null)
        {
            foreach (var item in Model)
            {
                 <tr class="text-center">
                     <td>
                         @Html.DisplayFor(modelItem => item.num_serie)
                     </td>
                     <td>
                         @Html.DisplayFor(modelItem => item.fecha_compra)
                     </td>
                     <td>
                         @Html.DisplayFor(modelItem => item.monto)
                     </td>
                     <td>
                         @Html.DisplayFor(modelItem => item.id_e)
                     </td>
                     <td>
                         @Html.DisplayFor(modelItem => item.id_p)
                     </td>
                 </tr>
            }
        }

    }
</table>

<hr style="background-color:white;">
<div style="padding:10px; color: white; text-decoration: none;">
    <a href="@Url.Action("VistaReporteCompras", "ReportesCP", new { id = "pdf" })" class="btn btn-danger" style="border-color: transparent;">
        Reporte PDF
    </a>
    <a href="@Url.Action("VistaReporteCompras", "ReportesCP", new { id = "Excel" })" class="btn btn-success" style="border-color: transparent;">
        Reporte EXCEL
    </a>
    <a href="@Url.Action("VistaReporteCompras", "ReportesCP", new { id = "Word" })" class="btn btn-primary" style="border-color: transparent;">
        Reporte WORD
    </a>
</div>