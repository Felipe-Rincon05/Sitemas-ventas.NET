@model IEnumerable<PROYECTO_WEB.Models.proveedores>

@{
    ViewBag.Title = "Index";
}

<h2 style="color:white">PROVEEDORES</h2>
<br />
<table class="table table-dark table-sm align-middle">
    <tr class="text-center">
        <th>
            ID PROVEEDORES
        </th>
        <th>
            NOMBRE
        </th>
        <th>
            TELEFONO
        </th>
        <th>
            DIRECCIÓN
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr class="text-center">
            <td>
                @Html.DisplayFor(modelItem => item.Id_proveedores)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.telefono)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.direccion)
            </td>
            <td>
                @Html.ActionLink("Editar", "Edit", new { id = item.Id_proveedores }, new { @class = "btn btn-outline-warning", style = "border-color: transparent;" }) |
                @Html.ActionLink("Detalles", "Details", new { id = item.Id_proveedores }, new { @class = "btn btn-outline-info", style = "border-color: transparent;" }) |
                @Html.ActionLink("Eliminar", "Delete", new { id = item.Id_proveedores }, new { @class = "btn btn-outline-danger", style = "border-color: transparent;" })
            </td>
        </tr>
    }
</table>
<br />
<p class="btn btn-outline-success">
    <a href="@Url.Action("Create")" class="btn btn-link" style="text-decoration: none; color: inherit;">Nuevo cliente</a>
</p>