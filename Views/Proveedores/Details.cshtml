@model PROYECTO_WEB.Models.proveedores

@{
    ViewBag.Title = "Details";
}

<h2 style="color: white">DETALLES PROVEEDORES</h2>

<div style="color:white">
    <hr />
    <dl class="dl-horizontal">
        <dt>
            ID PROVEEDORES
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Id_proveedores)
        </dd>

        <dt>
            NOMBRE
        </dt>

        <dd>
            @Html.DisplayFor(model => model.nombre)
        </dd>

        <dt>
            TELEFONO
        </dt>

        <dd>
            @Html.DisplayFor(model => model.telefono)
        </dd>

        <dt>
            DIRECCIÓN
        </dt>

        <dd>
            @Html.DisplayFor(model => model.direccion)
        </dd>

    </dl>
    <a href="@Url.Action("Edit", new { id = Model.Id_proveedores })" class="btn btn-warning" style="border-color: transparent;">Editar</a>
</div>
<br />
<br />
<p class="btn btn-outline-primary">
    <a href="@Url.Action("Index")" class="btn btn-link" style="text-decoration: none; color: inherit;">Volver</a>
</p>